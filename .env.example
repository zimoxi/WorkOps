BACKUP_MANAGER_DATA=/app/data

# The web UI stores executor settings in data/config.json.
# Recommended modes:
# mock  - local Windows testing, no real backup commands are executed
# local - run commands on the same Linux host
# ssh   - run white-listed commands through SSH
